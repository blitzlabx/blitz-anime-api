services:
  - type: web
    name: blitz-anime-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20.18.0
      - key: PORT
        value: 10000
    healthCheckPath: /health
    autoDeploy: true
